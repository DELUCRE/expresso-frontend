{
  "name": "expresso-frontend",
  "version": "3.0.1",
  "description": "Frontend da aplicação Expresso Itaporanga - CORRIGIDO",
  "main": "public/index.html",
  "type": "module",
  "scripts": {
    "start": "npx --yes serve@latest -s public -l $PORT --cors",
    "dev": "npx --yes serve@latest -s public -l 3000 --cors",
    "build": "echo 'Static site - no build required'",
    "test": "echo 'No tests specified'"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": ["logistica", "delivery", "frontend"],
  "author": "Expresso Itaporanga",
  "license": "MIT"
}
